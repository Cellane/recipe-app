spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/spring-prod
    username: spring-prod
    password: password
    initialization-mode: always
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false
